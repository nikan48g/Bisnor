# Example Keystore Configuration for Bisnor Release Builds
# Copy this file to `keystore.properties` and fill in your actual production keystore credentials.
# `keystore.properties` and `*.keystore` are ignored by git to protect private keys.

storeFile=release.keystore
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password

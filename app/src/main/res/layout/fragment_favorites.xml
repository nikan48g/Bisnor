<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical"
    android:paddingTop="16dp">

    <!-- Top Header with title and + Create Button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/vazirmatn_bold"
            android:text="لیست‌ها و کالکشن‌ها"
            android:textColor="@color/on_surface"
            android:textSize="22sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_create_playlist"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/vazirmatn_medium"
            android:text="+ ایجاد لیست"
            android:textSize="12sp"
            app:backgroundTint="@color/surface_container_high"
            app:cornerRadius="12dp" />
    </LinearLayout>

    <!-- Watched / Unwatched / All Filter Chips -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:clipToPadding="false"
        android:paddingHorizontal="16dp"
        android:scrollbars="none">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chip_group_watch_filters"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:singleSelection="true">

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_filter_all"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checked="true"
                android:fontFamily="@font/vazirmatn_medium"
                android:text="همه لیست‌ها (All)"
                android:textColor="@color/on_surface" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_filter_unwatched"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/vazirmatn_medium"
                android:text="دیده‌نشده‌ها"
                android:textColor="@color/on_surface" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_filter_watched"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/vazirmatn_medium"
                android:text="✓ دیده‌شده‌ها"
                android:textColor="@color/on_surface" />
        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

    <!-- Vertical List of Playlist Cards -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_playlist_cards"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="8dp"
        android:clipToPadding="false"
        android:paddingBottom="24dp" />

</LinearLayout>
